apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'signing'
apply plugin: 'idea'

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

group = 'com.esyfur'
version = '0.1'

defaultTasks 'clean', 'build'

dependencies {
    groovy localGroovy()
    compile gradleApi()
}
