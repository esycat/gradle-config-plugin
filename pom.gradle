/**
 * Create POM config and return for use by other tasks.
 */
def getPomConfiguration() {
    return {
        name 'Gradle Expand Properties Plugin'
        description 'Gradle plugin to expand dotted properties into nested maps.'
        url 'https://github.com/esycat/gradle-expand-props'
        packaging 'jar'
        licenses {
            license {
                name 'The Apache Software License, Version 2.0'
                url 'http://apache.org/licenses/LICENSE-2.0'
                distribution 'repo'
            }
        }
        developers {
            developer {
                id 'esycat'
                name 'Eugene Janusov'
                email 'esycat@gmail.com'
            }
        }
        scm {
            url 'https://github.com/esycat/gradle-expand-props'
            connection 'scm:https://github.com/esycat/gradle-expand-props.git'
            developerConnection 'scm:git@github.com:esycat/gradle-expand-props.git'
        }
    }
}
